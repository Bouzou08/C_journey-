#include <stdio.h>

int main()
{
    int arr[] = {1, 2, 4, 5, 6};
    int ntf;
    int found = 0;
    printf("not found number \n");

    for (int i = 0; i < 5; i++)
    {
        found = 1;
    }

    if (arr[] == ntf)
    {
        printf("you number %d is found\n", ntf);
    }
    else
    {
        printf("your number %d is not found \n", ntf);
    }
}
